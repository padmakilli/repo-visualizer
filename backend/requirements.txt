fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
httpx==0.28.1
python-dotenv==1.0.1

# Optional provider SDKs. The app also works with raw HTTP via httpx,
# so these are only needed if you prefer the official SDKs.
anthropic==0.42.0

# Dev / test
pytest==8.3.4
